# Upstash Redis (https://upstash.com) — required.
# Used for both scan-result persistence (shareable /scan/[id] links) and
# rate limiting on /api/scan.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
